[build-system]
requires = ["setuptools>=64", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "Ref2Act"
version = "0.1.0"

[project.scripts]
convert = "Ref2Act.convert_gmr_data:main"